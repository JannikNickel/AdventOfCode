#include "solutions.h"
#include "common.h"

static vector parse_input(const input* input);

result day00_part1(const input* input)
{
	return result_none();
}

result day00_part2(const input* input)
{
	return result_none();
}

vector parse_input(const input* input)
{
	vector v = vector_create(sizeof(uint64_t));

	return v;
}
